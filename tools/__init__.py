"""Local helper tools for building, running, and analyzing k6 Fargate load tests.

This folder is intentionally lightweight (not a published package), but having it
as a Python package enables running tools via `python -m tools.<name>` and makes
imports behave consistently.
"""
